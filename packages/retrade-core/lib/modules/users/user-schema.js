/*
import { addGraphQLSchema } from 'meteor/vulcan:core';


const userSchema = `
  type Profile {
    location: String
  }

  type User {
    name: String
    email: String
    password: String
    profile: Profile
  }
`;

addGraphQLSchema(userSchema);

*/
